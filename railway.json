{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd src && npm install"
  },
  "deploy": {
    "startCommand": "cd src && bash start.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
